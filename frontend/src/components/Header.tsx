// will contain navbar and can contain static content like hero section if needed

import Navbar from "./Navbar";

export default function Header() {
  return (
    <div>
      <Navbar />
    </div>
  );
}
